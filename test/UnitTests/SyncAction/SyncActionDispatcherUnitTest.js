
'use strict'

const expect = require('chai').expect
const SyncActionDispatcher = require('../../../SyncAction/SyncActionDispatcher');

describe('SyncActionDispatcher module', () => {
  describe('"dispatch"', () => {
    it('should allow clients to access the tag name', function() {
      throw new Error('Needs unit test.')
    });
  });
})
